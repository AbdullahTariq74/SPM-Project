# Conventions

## Code Style
- Uses ESLint for frontend code (`eslint.config.js`).
- Tailwind CSS utility classes used for styling components.

## Patterns
- API relies on standard Express.js middleware and routing.
- Database is initialized via SQL scripts mounted in Docker entrypoints.
